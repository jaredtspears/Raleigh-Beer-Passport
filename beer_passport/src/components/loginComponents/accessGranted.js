import React from 'react'

const AccessGranted = () => {
  return ( 
    <div>
      <p>
        Return access to full site (should be parent to the sites components)
      </p>
    </div>
   );
}
 
export default AccessGranted;